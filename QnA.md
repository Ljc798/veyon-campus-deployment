# 常见问题 Q&A

## 新增 App 能直接部署吗？

当前 App 为 0.4.5 Windows 离线部署实验版，具有 Veyon、改名、账户和网站策略相关执行路径；普通学生账户密码可以留空，管理员改密默认目标名为 `Administrator`。构建与 34 项自动检查通过，真实 Windows 部署验收仍待完成。它不会调用下面的旧 PowerShell 脚本。当前进度见 [App 文档入口](docs/README.md)，部署和测试见 [开发与部署指南](docs/开发与部署指南.md)。以下问题主要针对原脚本学习版。

## App 目前有哪些可选操作？

学生端可分别选择 Veyon 部署、改名、创建普通学生账户和修改管理员密码，也支持组合；普通学生账户密码可留空，管理员目标名默认 `Administrator`。教师端网站黑白名单支持 Edge/Chrome。代码和自动检查已完成相应部分，系统级操作仍要先在可还原测试机验证。任务和验收标准见 [开发路线与任务清单](docs/开发路线与任务清单.md) 和 [测试清单](docs/测试验收与发布清单.md)。

## 1. 这是真正的一键部署吗？
目前是交互式配置脚本。安装 Veyon、复制运行脚本、教师端权限及连接验收仍需人工完成；电脑列表可用 `add_computer_v1.txt` 批量导入。先按 [README](README.md) 做一台试装。

## 2. 教师端和学生端有什么区别？
教师端创建密钥及部署包，教师使用 Veyon Master 查看学生机。学生端读取部署包、修改本机账户和名称、设置认证并导入公钥。学生机不需要安装 Master。

## 3. TXT 文件怎么运行？需要改成 PS1 吗？
当前版本按 README 用记事本打开、全选复制，再粘贴到管理员 Windows PowerShell 运行。不需要改后缀，也不需要全局放宽执行策略。不要把脚本粘贴到 CMD。

## 4. 会覆盖已有管理员密码吗？
**会。**当前代码调用 `Set-LocalUser` 设置运行脚本的本地账户密码，没有“只在空密码时设置”的分支。使用其他管理员凭据提升权限时，运行身份也可能不同于桌面登录用户，务必核对确认页面。不要用域账户或 Microsoft 账户直接批量部署。

## 5. User 一定没有密码吗？会自动登录吗？
只有新建 `User` 时使用无密码方式。已有 `User` 的密码不会被清空，禁用状态也不会自动修复。脚本不设置自动登录。若 `User` 属于管理员组，脚本会报错，但此时可能已经修改计算机名和管理员密码。

## 6. 部署包路径填什么？
填包含 `campus.json`、`admin.txt` 和公钥的文件夹路径，例如 `E:\Veyon-Student-Deployment-JJ-CHAISANG`。换 U 盘后盘符可能不同；不要填单个文件路径。

## 7. 找不到 Veyon CLI 怎么办？
确认已经安装 Veyon。教师 / 学生部署脚本只查找 `C:\Program Files\Veyon` 下的 `veyon-cli.exe` 和 `veyon-wcli.exe`；新增导入脚本只查找前者。非默认安装路径尚不支持自动识别。

## 8. 学生端显示成功，教师端却看不到电脑？
脚本成功只表示本地步骤完成，不代表网络连接验收成功。按顺序检查：

1. 教师端“位置和计算机”是否已添加学生机。
2. 地址或主机名是否正确，学生机是否已重启、开机联网。
3. 学生机 Veyon 服务是否运行，双方实际配置端口是否可达。
4. 双方是否使用匹配的密钥认证配置，校区公钥是否对应教师机私钥。
5. 日常教师账户是否有私钥读取权限，以及学生机访问控制规则是否允许该账户。

默认服务端口是 TCP 11100。可在教师端 PowerShell 执行 `Test-NetConnection -ComputerName <学生机实际IP> -Port 11100`，将占位内容替换成真实 IP。端口测试成功仍需继续检查认证；不要通过永久关闭防火墙解决问题。

参考：[官方配置说明](https://docs.veyon.io/en/latest/admin/reference.html)。

## 9. 公钥和私钥可以一起复制给学生机吗？
学生机只需要公钥。私钥留在受控教师机，教师端通过它完成认证。公钥不是管理员密码，但 `admin.txt` 是敏感文件，整个部署包应由管理员保管。

## 10. 可以把整个文件夹发给别人看吗？
请分享本 GitHub 仓库链接，或包含教程成片的仓库源码 ZIP。公开内容不应包含实际校区的 admin.txt、公钥或私钥部署包。自己后续生成的实际部署包不要混入学习包；若正在使用的管理员密码曾被外发，应更换相关密码。视频已完成关键画面抽查；今后替换录像或截图时仍需检查敏感内容。

## 11. 多个机房能都从 PC-01 开始吗？
不要在可能相互通信的环境重复计算机名。给不同机房配置不同 `computerPrefix`，例如 `A-PC-`、`B-PC-`；最终名称不得超过 15 字符。默认编号范围是 1–99，当前脚本不检查全网重名。

## 12. 可以重复运行吗？失败后能直接再跑吗？
不保证重复执行完全无副作用。学生端会再次设置管理员密码并导入公钥；教师端遇到已有部署目录会停止。先确认已经完成哪些操作以及现有密钥情况，再决定如何继续。不要为解除报错盲目删除目录或重建同名密钥。

## 13. 报错后怎么反馈，怎么恢复？
提供 Windows 和 Veyon 版本、教师端还是学生端、最后成功步骤、完整错误信息及是否已重启。不要发送 `admin.txt` 内容或私钥。当前没有自动日志和回滚；先核对计算机名、管理员密码、`User` 状态、认证配置及服务状态，再按部署前记录恢复。

## 14. 不同校区管理员密码不同就互相隔离了吗？
不能这样判断。管理员登录密码与 Veyon 密钥认证是两套机制；是否能连接还取决于密钥、权限、访问控制及网络配置。需要跨校区隔离时应单独验收。

## 15. 换教师电脑或增加一名教师怎么办？
现有学生机信任原有公钥，重新生成一套密钥并不会自动接管它们。由管理员通过受控流程迁移所需私钥、权限和配置，或规划密钥更换并更新学生机。当前脚本不提供迁移与轮换流程。

## 16. 卸载软件能撤销所有修改吗？
不能。计算机名、本地账户密码及 `User` 账户设置需单独恢复；不要直接删除有文件或正在使用的账户。

## 17. 为什么公开版叫 v1.0，脚本却叫 v3？
v1.0 是首个公开学习包的版本；v3 是先前的内部迭代名称。为了能直接跟着已有视频操作，本包保留 teacher_v3.txt / student_v3.txt 文件名。直接使用包内脚本即可，不需要另找 v1 文件。

## 18. 哪些功能已经实现，哪些只是计划？
请看 [当前能力与未来计划](当前能力与未来计划.md)。当前提供交互式部署配置；账户可选操作、前置预检、自动回滚、完整日志及支持自定义地址的批量电脑清单等仍属于待开发方向；按固定 `PC-` 主机名批量导入已由新增脚本实现，详见 [使用说明](批量添加计算机.md)。其余待开发功能不能按已经实现的功能使用。


## 19. 怎么批量添加计算机？需要在学生机运行吗？
在教师机运行 `add_computer_v1.txt`，输入地点、数量和显示名称前缀，核对预览后输入 Y。它只管理教师端 Veyon 电脑目录，不在学生机运行，也不会修改学生机账户、机名或认证。详见 [使用说明](批量添加计算机.md)。

## 20. 为什么改了前缀，连接地址还是 PC-01？
前缀只决定列表中的显示名称。主机名固定为 `PC-01` 起，不读取部署包的 `computerPrefix`。自定义机名需更正“主机 / IP 地址”并应用保存；导入条目数量范围与学生部署编号范围也不同。

## 21. 视频改完名称为什么没有保存？应该怎么做？
这是原录像的操作遗漏。第 04 段剪辑版已经插入定格纠错：修改显示名称后，点击配置器底部“应用”保存，检查保存结果后再打开 Master。不能照着漏掉保存的步骤操作。

## 22. 导入失败后能直接再运行吗？
不要盲目重跑。脚本没有自动回滚、去重或合并；失败前可能已创建地点或部分电脑条目。先检查配置器内的实际结果和 CLI 报错，再由管理员修正。
