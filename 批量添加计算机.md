# 教师端批量添加计算机

对应脚本：[add_computer_v1.txt](add_computer_v1.txt) · [操作视频](视频教程/04_一键添加计算机_剪辑说明版.mp4)

这是交互式批量导入工具：创建一个 Veyon 地点，根据数量生成电脑条目并导入。它不会扫描网络、安装学生端、修改实际电脑名称、设置认证或验证远程连接。

## 运行前

- 在教师机安装 Veyon，使用管理员 Windows PowerShell；脚本自身没有管理员权限预检。
- 当前只查找 `C:\Program Files\Veyon\veyon-cli.exe`，不识别自定义安装路径，也不回退到 `veyon-wcli.exe`。
- 备份原有配置与电脑列表，并关闭已打开的 Veyon 配置器，导入后重新打开检查。
- 确认实际学生机采用 `PC-01`、`PC-02` 等主机名，而且教师机能够解析这些名称。

## 操作步骤

1. 用记事本打开脚本，全选复制，粘贴到管理员 Windows PowerShell 并运行。
2. 输入校区名称：这里是 Veyon 电脑目录中的地点名称，不会创建或更换认证密钥。
3. 输入学生电脑数量：整数 `1–999`，编号始终从 1 开始。
4. 输入显示名称前缀，例如 `学生`。地点和前缀不要包含分号或换行，当前脚本未处理这些 CSV 特殊字符。
5. 检查预览，输入 `Y` 或 `y` 并回车开始；其他输入取消。
6. 等待创建地点、生成临时 CSV、导入条目完成。导入调用返回后，脚本尝试删除临时 CSV。
7. 重新打开配置器，在“地点及计算机”中选择新增地点，检查条目数量、显示名称和主机 / IP 地址。
8. 如需编辑显示名称，修改后点击窗口底部的 **“应用”** 保存。重新打开配置器检查保存结果，再打开 Master。
9. 在 Veyon Master 选择对应地点，逐台核对连接。问号、离线或无画面的图标不能视为验收通过。

## 显示名称与连接地址

以数量 `20`、前缀 `学生` 为例：

| 编号 | 显示名称 | 连接主机名 |
| --- | --- | --- |
| 1 | 学生01 | PC-01 |
| 2 | 学生02 | PC-02 |
| 20 | 学生20 | PC-20 |

显示名称前缀不影响主机名。连接地址在脚本中固定为 `PC-` 加编号；100 及以上是 `PC-100` 等，不会截断成两位数。脚本不读取学生部署包中的 `computerPrefix`，不支持指定起始编号、导入现成 IP 清单或填写 MAC 地址。

如果实际主机名为 `A-PC-01`，仅把显示名称改成 `A-PC-01` 不能改变连接地址；需要在配置器中更正“主机 / IP 地址”，点击“应用”保存。现有学生部署脚本仍只支持编号 1–99，导入 999 个条目不代表已部署 999 台电脑。

## 视频中的漏保存操作

原录像修改了前两台电脑的显示名称，但忘记点击底部 **“应用”**。新版视频在改名后插入约 8 秒定格，标出按钮位置并说明漏做的步骤；没有补造已经保存成功的操作画面。

正确顺序：**编辑名称 → 点击“应用” → 核对保存结果 → 打开 Veyon Master**。这一提醒针对配置器内手动编辑的更改；脚本的 CLI 批量导入另有执行结果，需要分别核对。

## 出错与重复运行

- 找不到 CLI：检查是否安装在默认路径。
- 创建地点失败：可能是地点已存在，也可能有权限或其他 CLI 错误；先检查实际输出与已有目录，不要直接删除原地点。
- 导入失败：可能已经创建地点或导入部分条目，脚本没有自动回滚。
- 重复运行：没有去重、合并或可靠的幂等处理；先检查已有条目，再决定如何修正，不要盲目重跑。
- 条目出现但连不上：检查地址解析、学生机开机状态、网络、Veyon 服务、认证及私钥权限，参见 [Q&A](QnA.md)。

本次按脚本源码和用户提供的录像整理说明，未独立完成 Windows 实机兼容性测试。教师端认证配置和单机连接验收仍以 [README](README.md) 为准。
