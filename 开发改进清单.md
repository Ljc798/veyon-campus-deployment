# v1.0 开发改进清单（未实现事项）

本文供希望参与改进的读者参考；对外的能力与计划概览见 [当前能力与未来计划](当前能力与未来计划.md)。下述逻辑改造尚未实施。本次新增独立的批量添加计算机脚本及教程；原教师 / 学生部署脚本保持不变。静态检查与视频演示不等同于 Windows 跨环境测试，安装程序数字签名尚未核验。

## 优先级 1：后续逻辑改造重点

### 账户修改应与 Veyon 配置分开选择

当前学生脚本强制覆盖运行身份对应的本地账户密码，改名和账户调整也不可单独跳过。建议将“Veyon 配置”“计算机改名”“账户初始化”做成独立选项，密码覆盖需要单独明确确认。公开演示材料应与含 admin.txt 的实际部署包分开。

验收：只配置 Veyon 时，账户及电脑名不变；用户选择覆盖密码时，界面明确展示目标账户且不会泄露密码。

### 把全部前置检查放到第一次修改之前

现在先 Rename-Computer、Set-LocalUser，再检查 User 是否是管理员；后面报错时机器已部分修改。应提前检查 User 冲突、账户启用状态、服务存在、命名规则、部署包字段、文件类型及本地账户身份。

特别需要修正：通过 `$env:USERNAME` 加 `Get-LocalUser -Name` 只能匹配名称，不能可靠证明运行身份就是该本地用户；域账户与本地用户同名时可能误匹配。应核对运行身份 SID 与目标本地用户 SID，并明确拒绝不支持的账户来源。

验收：预检失败时，计算机名、密码、组成员及 Veyon 配置均未被修改。覆盖同名域用户、已有管理员 User、禁用 User、缺少服务及错误配置等场景。

### 完成教师端配置闭环

教师脚本目前创建密钥和文件，但没有设置教师端认证方式、私钥读取权限，也没有生成学生机列表。新增 `add_computer_v1.txt` 已提供独立的地点创建和固定 PC- 主机名批量导入。后续仍需整合认证、私钥权限、可配置地址和连接验收。

验收：日常教师账户可连接重启后的试装学生机，不依赖一直以管理员运行 Master。

## 优先级 2：批量部署可靠性

### 可重复执行、日志和失败恢复

教师脚本仅根据桌面部署目录是否存在来拦截重复操作，没有检查 Veyon 中是否已经有同名密钥；失败后可能残留目录或密钥。建议明确“新建校区”“复用现有密钥”“更换密钥”三种操作，并先完成已有状态检查。

学生端建议记录每个步骤结果、Veyon CLI 退出码和最终状态，出错后明确列出已生效操作。备份可恢复的配置；密码无法从系统读回，应单独设计恢复账户或重新设置流程。日志不能记录密码或私钥。

验收：在导入公钥、设置密码、重启服务等步骤模拟失败，能够定位原因并安全继续，不误报整体成功。

### 校验配置和统一密码读取规则

对 campus.json 做类型及格式校验，限制 keyFile 为部署目录内的预期公钥文件，校验 computerPrefix 的合法性及最终名称。当前教师端允许密码首尾空格，学生端读取却使用 Trim()，会造成两端密码含义不同；应统一规则并提前提示。

验收：非法前缀、越界公钥路径、空字段、超长名称、首尾空格密码都在修改系统前得到明确处理。

### 统一版本与运行入口

首个公开学习包定为 v1.0。为保持视频和操作文件一致，暂保留 teacher_v3.txt / student_v3.txt 名称及历史组件版本 0.4 / 0.7；对应关系已在 README 中说明。后续更新代码时再统一组件版本与入口。后续改成 UTF-8 BOM 的 .ps1，增加检查管理员权限的启动入口，减少复制粘贴截断问题；不建议全局关闭执行策略。

验收：在中文路径、带空格路径和非默认安装路径运行；明确记录支持的 Windows、PowerShell 和 Veyon 版本。

## 优先级 3：提高展示和交付质量

- 公开学习包已经与实际校区部署包分开；后续可增加可供教学使用的虚构配置示例。重新录制视频时继续检查凭据和个人信息。
- 当前已提供官方软件下载入口和文件哈希；后续补充安装程序来源核验与数字签名验证记录。
- 批量导入脚本需增加管理员权限预检、自定义主机前缀 / 起始编号、CSV 字符转义、去重、部分失败处理，以及与学生部署编号范围的一致性。
- 当前已有四段附带操作说明的视频；第 04 段增加了漏点“应用”的定格纠错；后续按实际测试结果维护截图与兼容性记录。
- 设计教师机迁移、密钥轮换和卸载后恢复说明。

## 本次已修正的文案问题

- README 引用不存在的 teacher_v1.txt，学生端步骤为空。
- 教师端提示及生成说明称“已有密码不会覆盖”，与学生脚本不符。
- 生成说明称“不修改 Veyon 配置”，与学生脚本不符。
- 学生端完成信息无条件声称 User 无密码，忽略已有账户保留密码的情况。
- 学生端确认界面未明确提示密码覆盖及失败无自动回滚。

这些文案修正不能替代上述逻辑改造；完成 Windows 单机试装验收后才能判断是否适合批量交付。
