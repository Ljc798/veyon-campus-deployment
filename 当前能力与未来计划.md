# v1.0 当前能力与未来计划

这是第一个面向公开学习的版本。本文把“脚本已实现什么”“哪些问题仍存在”“后续准备改什么”分开说明。已实现表示代码中存在对应流程，不代表已经在所有 Windows 环境验证。

## 一、目前可以解决的问题

| 常见问题 | v1.0 提供的处理方式 | 使用边界 |
| --- | --- | --- |
| 每次部署都要手动准备校区配置与公钥 | 教师脚本创建校区认证密钥，导出公钥并生成 campus.json、admin.txt、README.md | 先安装 Veyon；私钥保留在教师机 |
| 学生机名称不统一 | 输入 1–99 编号，按 computerPrefix 生成电脑名，例如 PC-03 | 重启后生效；不检查网络上的重名 |
| 需要统一学生机本地管理员密码 | 用部署包里的密码设置运行脚本对应的本地管理员账户 | 已有密码也会覆盖；不支持作为域账户或 Microsoft 账户部署方案 |
| 需要给学生准备普通账户 | 不存在 User 时创建无密码普通账户，配置组成员及密码修改限制 | 已有 User 保留原密码；管理员 User 会触发停止；禁用账户不自动启用 |
| 每台学生机都要手动设置认证并导入公钥 | 自动切换密钥认证、导入校区公钥并重启 Veyon 服务 | 教师端权限、访问规则及实际连接仍需检查 |
| 电脑条目逐台添加耗时 | 新增 add_computer_v1.txt，创建地点并批量导入 1–999 个条目 | 连接地址固定为 PC- 前缀；不会发现电脑或验证连接 |
| 新接触的人不知道先做什么 | 提供 README、Q&A 和部署／连接／批量导入四段教程 | 视频展示操作方法；各自环境需先做单机验收 |
| 不清楚鼠标左键、右键如何操作 | 视频加入鼠标图标、单击／双击／右键、键盘及等待提示 | 是按录屏画面编写的步骤指导，不是逐次输入事件记录 |

## 二、目前还不能解决的问题

1. **不能全程无人值守部署。**安装程序、输入参数、教师端认证配置、导入参数填写及连接验收仍有手动步骤。
2. **不能只配置 Veyon 而自动跳过账户修改。**现有学生流程会同时改名、设置管理员密码和配置 User。
3. **不能保证失败前没有改动。**部分检查发生在改名、改密码之后，中途失败不会自动恢复。
4. **不能保证重复运行无副作用。**再次运行可能重设密码、重新导入公钥；教师端仅拦截已有同名部署目录。
5. **不能自动处理所有账户和安装环境。**只查找默认 Veyon 路径，复杂账户策略、域／Microsoft 账户及其他环境尚未系统验证。
6. **不能自动完成批量发现、密钥迁移、密码轮换或卸载恢复。**这些需要管理员另行规划。
7. **不能用“脚本成功”代替实际连接验收。**名称解析、网络、防火墙、教师私钥权限等仍可能影响连接。

当前保管方式也有明确限制：`admin.txt` 存储明文密码，必须使用受控部署介质，不能随公开学习包分发。密码首尾空白会被学生脚本去掉，因此当前不要使用这种密码。

## 三、未来计划解决的问题

以下均为**待开发方向**，尚未包含在 v1.0 中；按使用反馈调整优先级，不承诺完成日期或特定下一版版本号。

| 优先级 | 未来要解决的问题 | 计划改进 | 完成后如何判断 |
| --- | --- | --- | --- |
| 优先 | 只想配置监控，却同时修改了账户 | 将 Veyon 配置、电脑改名、账户初始化拆为可选操作；密码覆盖单独确认 | 选择“仅配置 Veyon”时账户和电脑名不变 |
| 优先 | 报错时机器已改了一半 | 在第一次修改前集中检查账户 SID、User 冲突、服务、路径和配置字段 | 预检失败时系统状态不变 |
| 优先 | 失败后不知道改了什么、怎样继续 | 增加不含密码的步骤日志、已完成操作清单和可恢复配置备份 | 模拟中途失败后能定位问题并按步骤恢复 |
| 后续 | 教师端还有多项手动配置 | 辅助配置认证和私钥权限，完善现有批量导入并增加连接自检 | 日常教师账户能连接试装学生机 |
| 后续 | 重跑、复用校区或换教师机容易弄乱密钥 | 区分新建、复用和更换密钥，增加迁移及轮换流程 | 复用部署不破坏原有连接；轮换有明确验证步骤 |
| 后续 | 多机房容易重名、数量超出范围 | 让学生部署和批量导入统一支持机房前缀、编号范围及自定义地址 | 多机房清单无重名，配置能导入并核对 |
| 后续 | 非默认路径、密码格式和配置文件容易出错 | 完善路径识别、输入校验、密码读取规则和受控凭据方案 | 非法输入在修改系统前被发现 |
| 持续 | 复制脚本不方便，适用环境不够明确 | 增加正式运行入口、版本记录、兼容性测试和维护文档 | 对明确列出的环境完成试装、失败和重启测试 |

## 四、希望大家反馈什么

优先反馈能够复现的问题：在哪一步、什么 Windows 与 Veyon 版本、使用哪类账户、完整错误、报错前最后成功的操作，以及重启后是否仍然存在。请勿提交管理员密码、私钥或未经处理的个人信息。

详细反馈格式见 [发布说明](发布说明.md)，代码层面的分析见 [开发改进清单](开发改进清单.md)。
