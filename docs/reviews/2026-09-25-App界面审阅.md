# App 界面审阅（2026-09-25）

> 历史 UI 审阅：以下控件树与旧 EXE 身份保留原时点含义，不代表当前 0.4.4 的运行画面。本轮仅静态复审和本地构建／检查，未重新操作 UI；当前状态见 [0.4.4 架构复审](2026-09-25-0.4.4架构复审.md)。UX-01～05/08 的源码调整仍需对应原生交互验收。

## 预览对象与方法

发现正在运行的 `artifacts/windows-x64/VeyonCampus.exe`，窗口标题为“Veyon 校园部署助手”。该 EXE 文件时间为 2026-09-25 13:25:56，磁盘文件 SHA-256 为 `89044BF2AB9D27D4602C5F99C04DA9903D06E3B487BE8D09820B6D956BB280C4`。摘要仅标识当前磁盘 EXE，不代表整包或已加载进程的构建身份。

通过 Windows UI Automation 成功读取学生页控件树；截图接口连续返回 `SetIsBorderRequired failed: 不支持此接口 (0x80004002)`。尝试切换教师页时返回 `coordinate input geometry is unavailable`，未验证切换成功，随后停止界面输入。教师页意见来自 XAML／ViewModel 静态审阅。未操作安装、配置、改名、账户或包生成。

这不是完整视觉验收：颜色、实际间距、文字裁切、悬停、高 DPI、键盘交互和教师页运行表现均待截图／人工补验。运行产物不能证明最新工作区已构建通过。

## 实际观察（学生页控件树）

- 有“学生端配置／教师端准备”导航和部署资料拖拽区、选择目录、清空资料入口。
- 侧栏写“独立操作预览版 0.2.0”“尚未执行系统部署”，顶部却写“支持分步安装与部署”；操作区再次写“只显示计划，不执行修改”。能力说明直接冲突。
- 同时存在“生成部署预览”“只读环境检查”“① 安装 Veyon”“② 配置并部署 Veyon”四个动作，当前后两个禁用，但缺少独立的禁用原因说明。
- 读取到电脑编号 1、规划名称 PC-01、学生账户 User 等已有表单内容；未清空或修改用户输入。

## 建议与验收

| ID | 优先级／依据 | 建议 | 验收标准 |
| --- | --- | --- | --- |
| UX-01 | 首先处理／运行观察 | 统一版本和能力说明；未验收执行功能明确标“实验功能”，改名和账户标“仅可预览” | 所有提示、按钮和实际副作用一致；版本从同一来源读取 |
| UX-02 | 首先处理／运行观察＋代码 | 明确主流程：选择任务 → 补资料 → 检查与预览 → 确认执行 → 查看结果。保留安装／配置独立操作时，把它们定义为明确的任务，避免同时出现四个平级动作 | 每阶段一个清楚的主按钮，禁用原因可见；不允许绕过检查执行 |
| UX-03 | 优先／XAML | 将“选择操作”放在资料之前；仅做改名或账户预览时折叠无关包资料。未实现操作旁持续标注能力边界 | 新用户能判断当前可执行哪些操作；无需导入资料即可预览独立改名 |
| UX-04 | 优先／ViewModel | “清空资料”实际调用 Reset，会清空编号、账户、勾选项；改为只清除包，另设“重置表单” | 清除资料不丢其他独立任务输入；重置名称与影响一致 |
| UX-05 | 优先／XAML＋绑定 | 导入包的校区和前缀以资料摘要显示，修改时明确提示需要重新校验，避免编辑字段后包悄然失效 | 包失效原因紧邻资料显示；状态和执行按钮立即同步 |
| UX-06 | 优先／XAML＋执行代码 | 用逐步结果列表代替大段拼接文本：待执行／执行中／完成／失败／待核对／待重启；显示最后成功步骤及日志入口 | 失败时仍保留已完成事实；服务、公钥未确认不能显示总体成功 |
| UX-07 | 优先／教师页源码 | 教师流程以“校区与密钥 → 安装资源 → 包内容核对 → 生成”为主；显示创建／复用身份及公钥指纹，私钥不进入学生包 | 生成前可看清输出内容和位置；重复生成不无意创建新身份；先关闭 REV-01 |
| UX-08 | 后续／XAML 推断 | 四个水平按钮、长安装器路径及固定最小窗口宽度需响应式处理；按钮换行、路径栏使用可伸缩 Grid、主操作区避免随长报告滚出视野 | 1366×768 与 100%／150%／200% 缩放下关键控件可达；这是待验证风险，尚未观察到实际裁切 |
| UX-09 | 后续／教师页源码 | 清单增加序号、电脑名、连接地址、校验状态列；摘要保留数量和首尾，明确“预览”和“已写入” | 99／100／150 边界可核对，不能把生成清单显示成已添加电脑 |
| UX-10 | 后续／源码 | 将“当前开发顺序”、manifest 字段、SID 等技术细节移入帮助／诊断；主界面说明用户要做什么、将改什么 | 首次使用者无需理解内部任务阶段和协议即可完成已支持流程 |

## 建议保留

保留角色入口、任务独立选择、资料拖拽、目标电脑名即时反馈和操作说明入口。XAML 已有侧栏、卡片与统一样式基础，可沿用；是否美观、对比度与布局是否合格仍需真实截图验证。

## 下一次界面验收

- [ ] 修复构建后，用可追溯产物重新检查学生／教师两页。
- [ ] 补齐默认、资料错误、有效包、预检阻断、执行中、部分完成、待重启的截图。
- [ ] 验证任务选择、资料清空、导航往返、禁用原因、键盘焦点与窄屏布局。
- [ ] 教师页先完成包生成安全修复，再验收输出目录及包内容。
- [ ] 以“一教师＋一学生重启后仍可连接”作为功能验收；界面检查不替代系统验证。

## 2026-09-25 界面修订记录

| ID | 本轮处理 | 当前状态 |
| --- | --- | --- |
| UX-01 | 侧栏统一写“App 预览版”并从程序集读取版本；实验执行、仅可预览的边界继续明确 | 源码已改并构建；截图和各页面文案一致性仍待目视确认 |
| UX-02 | 学生页按“选择操作 → 资料 → 计划与检查 → 确认执行”组织；两条执行路径分别显示缺少的执行条件 | 源码已改并构建；各门槛切换行为和窄屏布局仍待 UI 验收 |
| UX-03 | 操作选择移到资料之前；未选择 Veyon 时收起部署包区域，同时保留校区和前缀输入 | 源码已改并构建；独立改名／账户预览仍待界面验收 |
| UX-04 | “清除部署包”保留其余表单输入和操作选择；另设“重置表单” | ViewModel 和界面入口已改并构建；点击验收仍待补 |
| UX-05 | 导入状态增加校区／前缀摘要；修改二者时清除包上下文，并提示重新载入；同步失效计划和预检 | 源码已有并完善提示、执行门槛更新；状态变化仍待界面验收 |
| UX-06 | 暂未改为逐步结果列表；当前结果仍以多行文本显示 | 待做 |
| UX-07 | 教师密钥复用/新建和学生包公钥导出已接入，尚未完成 Windows Veyon CLI 与错误状态验收 | 按 REV-01 在可恢复测试机核对 CLI 密钥清单、权限及失败恢复 |
| UX-08 | 执行区改为可换行按钮，安装器路径选择区改用可伸缩 Grid | 源码已改并构建；1366×768 与 100%／150%／200% 缩放仍待视觉验收 |
| UX-09 / UX-10 | 教师清单列和主界面技术术语整理尚未处理 | 待做 |

本轮使用 .NET SDK 10.0.401 完成 solution Debug 构建和最新 App 项目构建（输出 `.tmp-build/preview-final`），均为 0 warning / 0 error；未运行检查程序。此前构建的 `src/VeyonCampus.App/bin/Debug/net10.0/VeyonCampus.exe` 窗口已启动，最新输出尚未重新启动；窗口截图连续返回 `SetIsBorderRequired failed: 不支持此接口 (0x80004002)`。因此确认当前源码可构建、此前构建副本可启动，但当前最新输出的启动和界面视觉尚未确认；本轮没有执行安装、配置、改名或账户操作。
