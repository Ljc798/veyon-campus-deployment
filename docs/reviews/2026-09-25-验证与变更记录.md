# 验证与变更记录（2026-09-25）

## 最新复核：2026-09-26 b66751e 审阅修复

Windows Release 构建通过（0 警告、0 错误），29 组本地检查通过。公钥快照已接入实际导入，最终验证重新参与汇总，修复进程结果分类及 Windows 测试入口；改名改用有效命令与明确读回，账户创建／改密恢复为仅预览。详见 [修复记录](2026-09-26-b66751e修复记录.md)。系统修改采用模拟进程，未执行真实改名或账户修改；以下历史记录保留原时点描述，以本节及修复记录为当前状态。

## 最新复核：2026-09-26 任务租约、资源快照与组合执行入口

本节为 2026-09-26 本地实现与检查结果；下方 2026-09-25 历史记录保留原时点描述，不与本节合并为一次验证。

| 项目 | 本次证据与边界 |
| --- | --- |
| 工作区 | `~/Documents/veyon-campus-deployment`；App 项目版本 0.4.4（未递增） |
| Git 基线 | `develop` 分支，工作区含未提交新文件与修改，未提交或推送 |
| SDK | `.NET 10.0.401`（`~/.dotnet` 本地安装），macOS Apple Silicon，win-x64 目标构建通过 |
| 构建 | `dotnet build src/VeyonCampus.App -c Release --no-restore -r win-x64`，退出码 0，0 警告 / 0 错误；`dotnet build tests/VeyonCampus.Checks -c Release --no-restore` 通过 |
| 检查 | `dotnet run --project tests/VeyonCampus.Checks -c Release --no-build --no-restore`，退出码 0，All 24 checks passed（较 2026-09-25 的 21 组新增 3 组） |
| 新增检查 1 | 任务租约：并发入口互斥，持有期间第二请求被拒，释放后可再次获取，无死锁 |
| 新增检查 2 | 公钥资源快照：副本摘要固定；源文件在执行期间被替换不影响已验证副本；快照目录在释放后删除 |
| 新增检查 3 | 进程结果：`NeedsReviewResult` 正确携带"需核对"说明；真实进程调用成功读回退出码 |
| 代码变更 | 新增 `src/VeyonCampus.Core/ITaskLease.cs`（`ITaskLease`/`TaskLease`/`TaskGate`，AR-01）、`PackageResourceSnapshot.cs`（AR-03）、`DeploymentRunLog.cs`（`IProcessLauncher`/`ProcessOutcome`/`DefaultProcessLauncher`/`DeploymentRunLog`，AR-02/AR-05/P3-09）、`WindowsRenameAdapter.cs`（P5 本地实现）、`WindowsAccountAdapter.cs`（P6 本地实现）、`WindowsVeyonVerificationService.cs`（P4-07/AR-04）；修改 `MainViewModel.cs`（四个修改入口统一任务租约，组合执行入口开放，`DeploymentPlan.ComputerNameFor`）、`DeploymentPlan.cs`（新增 `ComputerNameFor` 静态方法）、测试程序（新增 3 组检查，修正组合执行入口断言） |
| 未执行 | Windows 实机改名／账户修改、受限 Worker、跨进程锁、备份与崩溃恢复、UI 验证、150 台实机、教师学生连接 |

## 最新复核：0.4.4 文档与架构审阅

本节为本次实际执行结果；下方历史内容从原文档迁入，保留旧时点描述，不与本节合并为一次验证。

| 项目 | 本次证据与边界 |
| --- | --- |
| 工作区 | `F:\github\veyon-campus-deployment`；App 项目版本 0.4.4 |
| Git 基线 | 读取 HEAD 和分支引用：`develop` / `b39a394edd17f25690b3bd524deb576357ddaa0a`；Git CLI 不可用，未验证工作区差异或干净检出 |
| SDK | `dotnet --version` 返回 10.0.401；使用已还原依赖 |
| 构建 | `dotnet build VeyonCampus.slnx -c Release --no-restore -v minimal`，退出码 0，0 警告 / 0 错误 |
| 检查 | `dotnet run --project tests/VeyonCampus.Checks -c Release --no-build --no-restore`，退出码 0，All 21 checks passed |
| 检查覆盖 | 命名、独立选择、不可变计划、表单失效、资料／公钥／路径校验、预检门槛、内嵌资源提取复用、密钥清单解析、模拟执行顺序与失败状态 |
| 组合解释 | 模拟执行循环覆盖 15 个非空选择；空选择在另一检查中拒绝。测试输出中的“16 种”不能解释为 16 种系统部署通过 |
| 已有 ZIP | `artifacts/VeyonCampus-0.4.4-win-x64-offline.zip`，89,974,036 字节；本次重新计算 SHA-256 为 `C404FDC3DB90063F4667BB56CD2F5DD8BF8EF0395D8BFA5D90E0FA7ECF14DBE7` |
| 产物边界 | 摘要与历史记录一致；本次没有重打包或重新核验 ZIP 内发布程序集与当前源码的一致性 |
| 未执行 | 真实安装、Veyon 配置、服务变更、密钥创建、账户／改名、UI、远程 CI、Windows 10/11 矩阵及教师学生连接 |

构建设置了 `AVALONIA_TELEMETRY_OPTOUT=1`、`DOTNET_CLI_TELEMETRY_OPTOUT=1`。检查程序会创建并清理临时测试文件、提取测试安装器及读取平台状态；它不是完全不写磁盘的检查，但没有执行上述系统部署操作。

本次复审结果见 [0.4.4 架构复审](2026-09-25-0.4.4架构复审.md)。未运行新的故障注入用例；新增 AR-T 项是待验收规格。docs 全部 Markdown 已检查严格 UTF-8 解码、本地相对链接和代码围栏配对；原 P2-06 包含未覆盖的进程状态采集，已恢复为未完整验收。

## 历史记录的阅读方式

以下从三个文档迁入，保留原内容以便追溯；其中“本轮”“当前”“未在 Windows 执行”等仅指当时记录。旧版 19/20 组检查与后续 21 组检查不矛盾，不得用旧版记录替代新代码验证。相对链接已按新位置修正。

## 原 app-development.md 验证记录

### 本次验证记录

- .NET SDK 10.0.401，0.4.3 Release solution build 通过，0 warnings / 0 errors；设置 `AVALONIA_TELEMETRY_OPTOUT=1` 避免构建遥测写入用户目录失败。
- 上一版 0.4.3 有 20 组本地检查通过，覆盖固定安装器资产元数据、内嵌资源离线提取/校验/缓存复用、schemaVersion=2 配置包解析和不携带 EXE、Veyon KeyId 映射与 `sc.exe` 数字服务状态解析、管理员权限门槛、执行计划不可变性、公钥校验/私钥拒绝及包污染拒绝。
- 0.4.3 的只读状态探测已在当前 Windows 主机实测：识别 `C:\Program Files\Veyon\veyon-cli.exe` 和 `VeyonService`，读取到文件版本 `4.11.2`，并按固定基线 `4.11.2.0` 确认为支持版本；该验证没有启动安装器或更改 Veyon 配置。
- 0.4.3 Windows x64 自包含发布目录和 ZIP 已由 `scripts/package-windows-offline.ps1` 生成；ZIP 含完整 App，发布后的 Core 程序集内 Veyon 资源大小和 SHA-256 均通过复核。
- macOS 原生交互与 Windows 实机界面仍待验收。
- Windows x64 历史预览产物曾在 Windows 10 10.0.19044 x64 基线上启动；0.4.3 当前预览已生成，界面与拖拽交互仍待人工验收。
- 2026-09-25 教师端重复安装拦截、360 提示、Veyon 受控密钥目录复用/缺失时新建、学生包仅导出公钥及中文默认目录修改后，solution 构建和本地检查结果以本轮追加记录为准。Veyon CLI 的实际清单格式和 Windows 端 create/export 权限仍须可恢复测试机验收；本轮不在当前计算机上运行安装器或改动 Veyon 配置。
- 离线安装器改造后，根目录固定版安装器作为 Core 程序集资源随完整发布版分发；新版学生 schemaVersion=2 配置包不重复包含 EXE。Windows 本机检查已成功从资源提取安装器，并通过固定 SHA-256、WinVerifyTrust 和签名证书指纹校验；Release 发布版 `VeyonCampus.Core.dll` 内资源大小为 16,782,856 字节，资源摘要与固定值一致。
- 本轮没有启动 Veyon 安装器或更改 Veyon 配置；真实安装行为、服务状态、公钥指纹读回、Windows UI 与完整教师学生连接流程仍待可恢复测试机验收。已有 Veyon 版本与固定基线不符或安装状态未知时会停止。
- 拖拽入口已实现并编译；操作系统文件管理器拖入的端到端成功／失败验收记录仍待补齐。
- `PlatformFacts.Collect()` 在 macOS 上返回"不适用"，不推断 Windows 状态；Windows 实机管理员身份、重启待办与磁盘空间记录仍待 P4 验证。

#### 0.4.4 本轮执行协调器验证

- .NET 10.0.401 Release solution build 通过，0 warnings / 0 errors；本地检查 21 项全部通过。
- 新增 `ExecutionCoordinator`，现有学生端 Veyon 配置流程经此协调器按冻结计划执行。改名和账户步骤仍在执行入口被拒绝；没有启动安装器或修改系统配置。
- 模拟执行器覆盖所有 15 个非空选择（四项操作的 16 种布尔组合减去空选择）、按依赖顺序执行、失败后跳过未开始步骤、重启后停止、步骤边界取消和异常后需核对。模拟通过不替代 Windows UAC、故障恢复或系统集成验收。
- 尚未完成受限 UAC Worker、跨进程任务互斥、持久运行记录／结构化日志、可恢复备份和崩溃恢复；当前组合计划可验证，但真实组合操作仍不可执行。
- 用户反馈教师端 Veyon 操作已成功；这条手工反馈不等于教师认证配置、密钥迁移／备份、日常教师账户权限或电脑目录功能已完成，也不替代可复现的验收记录。
- Windows x64 离线 ZIP 已生成，共 225 个文件条目；App EXE、Core DLL 均在包内，发布 DLL 中固定 Veyon 安装器资源重新提取校验通过。ZIP SHA-256：`C404FDC3DB90063F4667BB56CD2F5DD8BF8EF0395D8BFA5D90E0FA7ECF14DBE7`。生成包不代表 Windows 实机部署验收通过。


## 原测试清单记录

#### 2026-09-25 审阅补充（当前工作区）

审阅发现当前版本代码存在私钥进入学生包、执行入口绕过预检、未支持组合可落入执行、服务失败处理及版本文案等问题。2026-09-25 已修正包生成与敏感文件拦截、预检门槛、未支持操作拒绝、异步 UI、3010 与服务错误传播。后续又固定 Veyon 官方安装资产摘要和 Authenticode 发布者指纹，修正密钥导入标识，并补入不可变执行计划及状态汇总。当前工作区验证结果以本文末尾最新记录为准；Windows 信任链、Veyon 公钥读回及 Windows 实机验收仍未完成。历史检查不得代替当前工作区验证。150 台规则已在代码中，150 台实机验收仍未完成。详见 [审阅记录](2026-09-25-产品与架构审阅.md)。

- [ ] REV-T01 / REV-01：生成完整学生包并递归检查内容，确认无私钥、密码或敏感日志；正常及中途失败均检查。
- [ ] REV-T02 / REV-02：修复源码后构建和现有检查通过，绑定 Git 提交、版本、产物摘要。
- [ ] REV-T03 / REV-03：导入后替换安装器／公钥、计划过期、来源不可信、权限不足时，执行边界在首次修改前停止。
- [ ] REV-T04 / REV-04：认证方式正确但目标公钥缺失／不匹配，或服务重启失败时，不显示部署成功；独立核对版本与组件。
- [ ] REV-T05 / REV-05：所有含未实现操作的组合均被拒绝；安装失败不调用配置；3010 正确传播待重启状态。
- [ ] REV-T06 / REV-06：长任务中 UI 可响应，不重复启动；超时、断连与退出后保留已完成步骤和需核对状态。
- [ ] REV-T07 / REV-07：界面、程序集、开发说明、发布产物版本和能力文案一致。
- [ ] REV-T08 / REV-08：新包生成与执行链路的正常／失败契约检查通过，再完成真实系统集成。
- [ ] REV-T09 / UX-01～10：按 [界面审阅](2026-09-25-App界面审阅.md) 补截图、教师页、禁用原因、小屏／高 DPI 和键盘验收。
- [ ] REV-T10：一教师一学生完成安装、配置、拔除部署介质、重启及日常教师账号连接，失败恢复记录可核对。

本轮只成功读取运行中学生页控件树；截图接口不支持、教师页切换失败，不能记为完整 Windows UI 验收通过。未执行系统部署。

#### 2026-09-25 教师端安装与学生包密钥修订

- 教师端安装前检查已有 Veyon：检测到已安装时显示“检测到本机已安装 Veyon，请勿重复安装”并停止；界面在安装按钮旁明确提示先暂时退出 360 等杀毒软件、安装完成后立即恢复防护。
- 学生配置包生成改为查询 Veyon 的密钥清单，按稳定 KeyId 复用完整密钥对；仅清单确认缺失时调用 `authkeys create`，对部分密钥或无法识别的清单停止。生成时只调用 `authkeys export <KeyId>/public`，不导出教师私钥、不创建旁置私钥文件夹；默认目录为 `Veyon-学生配置包-<校区ID>`。
- 上述安装与密钥修订时，.NET 10.0.401 Release solution build 成功，0 warning / 0 error；`dotnet run --project tests/VeyonCampus.Checks --no-build` 通过，19 组检查全部通过。内嵌安装器与轻量配置包功能的最新验证记录在本文末尾。
- 生成了 Windows x64 自包含预览产物 `.tmp-build/preview-teacher-keyvault-current/VeyonCampus.exe`。未在本轮运行 Veyon CLI、创建密钥或安装软件；Veyon CLI 实际格式、密钥库权限、UI 视觉验收及完整 Windows 操作仍待可恢复测试机验证。

测试分层：纯规则检查 → ViewModel／执行器契约检查 → Windows 系统集成 → 教师学生连接 → 小批试点 → 发布包验收。上层通过不能代替下层，也不能用模拟 150 条记录代替 150 台实机连接证据。

0.4.3 之前的本地证据为 20 组检查，覆盖固定 Veyon 安装器元数据、内嵌资源离线提取／校验／缓存复用、轻量校区配置包、公钥校验／私钥拒绝、KeyId 映射、`sc.exe` 数字服务状态解析、执行计划不可变性与结果汇总、包污染拒绝和预检入口状态检查。当前 Windows 主机已通过 `WinVerifyTrust` 与固定签名证书检查；默认路径探测使用 `veyon-cli.exe`，并兼容 Windows 文件版本 `4.11.2`。真实 Veyon 安装、公钥读回、Windows UI、操作系统拖拽端到端、改名／账户／重启／连接及失败恢复验收仍缺。150 台规则已实现，设备规模实测未完成。

#### 2026-09-25 执行协调器补充

0.4.4 Release solution build 通过，0 warnings / 0 errors；21 项本地检查通过。新增假执行器覆盖 15 种非空操作组合，核对冻结计划步骤和依赖顺序；测试失败后未开始步骤被跳过、安装重启要求停止后续步骤、步骤边界取消以及执行器异常转为需核对。现有 Veyon 配置流程接入协调器。所有组合场景均为模拟执行，没有更改系统设置；改名／账户执行、受限 UAC、跨进程互斥、日志持久化及真实故障恢复仍未完成。

用户本轮反馈教师端 Veyon 操作已成功。未提供可复现的 Windows 环境和验收步骤，因此记录为用户手工反馈；教师认证、权限、备份／迁移、电脑目录与学生端连接闭环仍待验收。

0.4.4 Windows x64 离线发布目录及 ZIP 已生成：`artifacts/windows-x64-v0.4.4`、`artifacts/VeyonCampus-0.4.4-win-x64-offline.zip`。ZIP 有 225 个条目，含 App EXE 与 Core DLL；打包时从发布后的 Core DLL 重新提取嵌入安装器并通过固定大小和 SHA-256 校验。ZIP SHA-256：`C404FDC3DB90063F4667BB56CD2F5DD8BF8EF0395D8BFA5D90E0FA7ECF14DBE7`。此产物未在 Windows 测试机安装或进行真实 Veyon 部署。

#### 2026-09-25 离线安装器内嵌

- `veyon-4.11.2.0-win64-setup.exe` 作为嵌入资源随 `VeyonCampus.Core.dll` 发布。应用从程序集提取至本机缓存后，仍按固定大小、SHA-256 校验；Windows 还要求 Authenticode 和签名证书指纹匹配。下载代码及外部获取流程已移除。
- 新生成的 schemaVersion=2 学生校区配置包不再重复携带 16.8 MB 安装器，只保留公钥、命名配置和清单；schemaVersion=1 旧包继续兼容。学生端离线部署需要将完整 App 发布文件夹与对应校区配置包一起带到现场。
- Release solution build 成功，0 warning / 0 error；本地 `VeyonCampus.Checks` 20 组检查全部通过，新增内嵌资源离线提取/缓存复用和轻量配置包解析检查。
- 未在当前计算机运行 Veyon 安装器或更改 Veyon 配置。Windows 安装实机、病毒防护交互、服务状态和学生机部署仍待可恢复测试机验收。

本轮又增加管理员身份门槛检查：管理员明确通过才允许进入执行门槛，普通权限阻断，身份未知保留未知且不允许执行。Windows 下普通权限与 UAC 行为仍须实机验收。

只有改变了行为、发现新失败或存在未解决风险时才扩大／重复测试。纯文案和简单配色修改不要求新增镜像式单元测试；影响真实系统的逻辑必须有针对性失败测试。


## 原路线变更记录

### 变更记录（本次 2026-09-25）

| 任务 | 变更 | 负责人 | 开始／完成日期 | 关联提交 | 验证环境 | 未解决问题 |
| --- | --- | --- | --- | --- | --- | --- |
| P2-03 | 新增 2048–4096 位公钥范围检查；假 PEM、私钥、随机内容和短位长公钥均有明确拒绝结果 | 待分配 | 2026-09-25 | 待提交 | macOS 本地 13 组检查通过 | 所选 Veyon 能否识别 4.11.2 仍需 P4 实机验证 |
| P2-04 | 空值、错误类型、重复字段、未知版本及根路径变化的本地检查；`PackageContext.PackageFingerprint` 用于绑定预检与包 | 待分配 | 2026-09-25 | 待提交 | macOS 本地检查通过 | Windows 实机异常与 150 台实机仍待 P4/P8 |
| P2-05 | 路径越界、重解析点、符号链接与包外引用拒绝；`VerifyUnchanged` 执行前再次校验，新增 `PackageFingerprint` | 待分配 | 2026-09-25 | 待提交 | macOS 本地检查通过 | Windows 实机路径与 150 台并发仍待 P4/P8 |
| P2-06 | 新建 `PlatformFacts.Collect()`：采集系统版本、架构、管理员身份、重启待办（注册表）与系统盘空间，全部只读 | 待分配 | 2026-09-25 | 待提交 | macOS 编译通过；Windows 实机待 P4 | Veyon 服务与 CLI 状态仍待 P4/P7 实机接入 |
| P2-07 | 新建 `VeyonFacts.Probe()`：默认路径（Program Files / Program Files (x86) 下 `Veyon\veyon-cli.exe`）与 `VeyonService` 服务探测、版本读取；未安装时预检显示“需要安装”提醒而非阻断 | 待分配 | 2026-09-25 | 待提交 | 0.4.3 按实际 Veyon CLI 文件名修正默认安装路径探测；Windows 文件版本三段式 `4.11.2` 与固定基线 `4.11.2.0` 兼容 | 非默认路径和损坏安装的完整分类仍未完成；服务未注册/状态未知现会按未知安装阻断执行 |
| P3-02a | 检查四项操作 15 种非空组合的冻结计划顺序和未选项隔离 | 本轮协作 | 2026-09-25 | 未提交（工作区） | Windows x64 / .NET 10.0.401 Release；21 项检查通过 | 跨进程互斥仍待 P3-02/P3-05 |
| P3-13a | 新增可注入执行协调器并用模拟处理器验证失败停止、待重启、步骤边界取消和需核对 | 本轮协作 | 2026-09-25 | 未提交（工作区） | Windows x64 / .NET 10.0.401 Release；solution build 0 warning / 0 error；21 项检查通过 | UAC、文件锁定、重复点击及真实 Windows 故障注入仍未完成 |

#### 审阅修复记录（2026-09-25）

| 审阅项 | 本轮实现 | 状态与边界 |
| --- | --- | --- |
| REV-01 | 学生包临时目录构建；匹配密钥由 Veyon 受控目录保管；生成时检查 Veyon CLI 密钥清单，复用完整密钥对，仅在确实缺失时创建，只导出公钥；学生包根目录白名单；新版 manifest 导入拒绝 admin/private/secret 文件和重解析点 | 本地公钥校验、私钥 PEM 拒绝、密钥清单状态解析和包污染检查通过；Windows CLI 实测及中途故障恢复仍待验收 |
| REV-02 / REV-07 | 项目版本更新至 0.4.1；侧栏从程序集读取版本；界面与 README 说明实验能力边界 | Release 构建通过，0 warning / 0 error；Windows UI 截图待复验 |
| REV-03 | 执行入口要求五分钟内、与当前输入完全一致且无阻断项的预检；执行前冻结计划并重验资料；安装器来源和签名需可信 | 本地 19 组检查通过；固定发布 SHA-256、WinVerifyTrust 与签名证书 SHA-256 已实现，Windows 信任链实测、可信快照与权限测试待完成 |
| REV-04 | 服务名统一为 `VeyonService`；`sc.exe query` 区分存在/明确不存在/未知；3010 与服务失败停止后续；认证方式不再冒充公钥指纹验证 | 本地 19 组检查通过；Veyon 密钥库指纹读回与 Windows 实测待完成 |
| REV-05 | 当前仅接受单独 Veyon 操作；改名/账户及组合选择在调用适配器前拒绝；安装失败不继续配置 | 本地 19 组检查通过；真实安装器故障与服务失败注入待 Windows 环境验证 |
| REV-06 | 预检、安装/配置和服务读回放入后台任务，UI 异步等待；预检请求版本阻止旧结果覆盖当前输入 | Release 构建通过；安全取消、持久运行记录和恢复待 P3 后续实现 |
| REV-08 | 新增学生包正常输出/敏感污染检查和预检入口状态检查 | 本地 19 组检查通过；模拟执行器与完整故障注入仍待实现 |

#### 界面审阅项跟进（2026-09-25）

| 任务 | 本轮处理 | 状态与证据 |
| --- | --- | --- |
| UX-01 / UX-02 | 学生页统一版本与能力说明，整理“选择操作 → 资料 → 预览和只读检查 → 确认执行”；两种执行路径显示当前门槛 | 源码已改；本轮 Debug solution build 通过，0 warning / 0 error；窗口截图和门槛切换仍待验收 |
| UX-03 / UX-04 | 操作区移到部署资料之前；不选 Veyon 时收起资料区；“清除部署包”保留其他输入，另设完整表单重置 | 源码及 ViewModel 已改；真实点击和独立操作预览待验收 |
| UX-05 | 导入摘要显示校区、前缀与公钥指纹；编辑校区或前缀会让旧包失效，重新导入并检查前清除执行条件 | 源码已改；状态同步仍待窗口交互验收 |
| UX-08 | 学生执行按钮改用可换行布局，教师安装器路径改用可伸缩 Grid | 源码已改；不同屏幕和 DPI 的实际可达性待验收 |
| UX-06 / UX-07 / UX-09 / UX-10 | 逐步结果列表、教师端密钥工作流和目录表格、主界面术语整理 | 未完成，继续按界面审阅记录推进 |

本轮最新 Debug 构建后，App 窗口已启动；截图捕获返回 `SetIsBorderRequired failed: 不支持此接口 (0x80004002)`，无法目视确认当前布局。未运行检查程序，也未执行任何系统修改。详细记录见[界面审阅](2026-09-25-App界面审阅.md)。
