# b66751e 审阅修复记录

日期：2026-09-26。基线：`b66751e3a3bba19d9ff5bb9d1513648995e42a60`。本记录描述工作区修复，不代表已提交或发布。

对应 [代码审阅报告](2026-09-26-b66751e代码审阅.md)。原报告保留被审阅提交时的事实。

## 修复与限制

| 审阅项 | 当前处理 |
| --- | --- |
| 1. 快照未参与导入 | `ConfigureVeyonOnly` 接收并消费公钥快照；实际 `authkeys import` 使用副本路径。Windows 全程保持只读共享句柄，阻止副本改写／删除；正常及提前返回时释放并清理。安装器仍使用原有缓存和信任校验，不属于公钥快照。 |
| 2. 验证未影响整体状态 | 配置成功后将最终读回转换成验证步骤，重新汇总，再写最终日志；保留待重启标志。当前独立公钥指纹读回尚未实现，因此不能仅凭命令返回成功宣布部署完全成功。 |
| 3. 错误改名命令 | 使用 `Rename-Computer`，通过 CIM 读取明确的域成员布尔值，通过注册表读回活动及待生效名称。不自动重启；域状态未知、存在其他待生效名称或读回不符时停止／需核对。 |
| 4. 错误 SID 来源 | 使用 `Get-LocalUser` 的结构化名称与 SID，要求名称匹配，不再解析 `net user` 文本。查询失败或字段不完整时不猜测 SID。 |
| 5. 无密码创建学生账户 | 移除无密码创建路径；在密码输入及策略校验完成前恢复为仅预览。预检、执行入口、适配器三处阻断，混合计划在任何修改前整体拒绝。 |
| 6. 改密传入空串 | 移除执行器空密码调用及适配器命令行密码路径；改密仅预览。双次密码输入、身份确认和密码策略流程仍待实现。 |
| 7. 非零退出码误报成功 | 包装器按退出码返回 Success / Failed，捕获 Win32 启动失败；超时维持状态待核对。域查询失败或缺少布尔字段不再默认当作工作组。 |
| 8. Windows echo 测试失败 | 使用检查程序自身作为可移植子进程，覆盖 0 / 非零退出码、可执行文件缺失和超时；不依赖 shell 内置命令。 |

另外将租约并发测试改为显式事件同步，避免依赖 `Thread.Sleep` 调度；测试不会对有效的真实改名计划调用执行入口。运行日志用覆盖移动代替先删旧文件再移动，并记录待重启标志。界面与当前能力文档同步显示账户仅预览。

## 验证

Windows 工作区使用 `C:/Program Files/dotnet/dotnet.exe`：

```powershell
dotnet build VeyonCampus.slnx -c Release --no-restore -v quiet
dotnet run --project tests/VeyonCampus.Checks -c Release --no-build --no-restore
```

- Release 构建通过，0 警告、0 错误。
- Windows 全部 29 组检查通过。
- 另做当前 Windows 只读命令核验：本地账户名称匹配、SID 可读取、域状态为明确布尔值、活动／待生效名称可读取，且 `Rename-Computer` 命令存在。CIM 查询在沙箱内拒绝访问，经授权在沙箱外只读复核通过；未调用改名命令。
- 新增／增强回归覆盖：实际快照导入路径与源替换、Windows 副本写入阻断、账户所有组合阻断、结构化 SID 与名称匹配、域未知状态、正确改名命令与精确待生效名称、已有改名待办、幂等、超时及最终验证／日志一致性。
- 真实子进程仅执行测试程序自身；Windows 改名与账户操作使用模拟进程。未对当前机器执行改名、创建账户、改密、安装或服务修改。

尚未完成：真实改名及重启后验收、Veyon 公钥指纹独立读回、账户密码与身份确认流程、教师学生连接及机房验收。账户相关审阅项按“关闭不完整执行入口”修复，不表示账户部署功能已经完成。

## 命令依据

- [Microsoft：Rename-Computer](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.management/rename-computer?view=powershell-5.1)
- [Microsoft：Get-LocalUser](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.localaccounts/get-localuser?view=powershell-5.1)
- [Microsoft：Win32_ComputerSystem.PartOfDomain](https://learn.microsoft.com/en-us/windows/win32/cimwin32prov/win32-computersystem)
