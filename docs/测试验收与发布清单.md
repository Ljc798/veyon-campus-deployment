# App 测试验收与发布清单

更新日期：2026-09-25。配套 [开发任务清单](开发路线与任务清单.md) 和 [架构约束](架构与实现约束.md)。本文件中的未勾选项目都是待验证项目，不是通过声明。

## 1. 测试原则与当前证据

### 2026-09-25 审阅补充（当前工作区）

最新审阅构建失败：MainViewModel 多处字符串损坏，报 CS1010／CS1003 等。因此下面“编译、十四组检查”的表述仅作为历史记录，不能用于当前工作区放行。1–150 规则已存在于源码；后文“150 台还没有改入代码”属于过时描述，不代表真实现状，150 台实机验收仍未完成。详见 [审阅记录](reviews/2026-09-25-产品与架构审阅.md)。

- [ ] REV-T01 / REV-01：生成完整学生包并递归检查内容，确认无私钥、密码或敏感日志；正常及中途失败均检查。
- [ ] REV-T02 / REV-02：修复源码后构建和现有检查通过，绑定 Git 提交、版本、产物摘要。
- [ ] REV-T03 / REV-03：导入后替换安装器／公钥、计划过期、来源不可信、权限不足时，执行边界在首次修改前停止。
- [ ] REV-T04 / REV-04：认证方式正确但目标公钥缺失／不匹配，或服务重启失败时，不显示部署成功；独立核对版本与组件。
- [ ] REV-T05 / REV-05：所有含未实现操作的组合均被拒绝；安装失败不调用配置；3010 正确传播待重启状态。
- [ ] REV-T06 / REV-06：长任务中 UI 可响应，不重复启动；超时、断连与退出后保留已完成步骤和需核对状态。
- [ ] REV-T07 / REV-07：界面、程序集、开发说明、发布产物版本和能力文案一致。
- [ ] REV-T08 / REV-08：新包生成与执行链路的正常／失败契约检查通过，再完成真实系统集成。
- [ ] REV-T09 / UX-01～10：按 [界面审阅](reviews/2026-09-25-App界面审阅.md) 补截图、教师页、禁用原因、小屏／高 DPI 和键盘验收。
- [ ] REV-T10：一教师一学生完成安装、配置、拔除部署介质、重启及日常教师账号连接，失败恢复记录可核对。

本轮只成功读取运行中学生页控件树；截图接口不支持、教师页切换失败，不能记为完整 Windows UI 验收通过。未执行系统部署。

测试分层：纯规则检查 → ViewModel／执行器契约检查 → Windows 系统集成 → 教师学生连接 → 小批试点 → 发布包验收。上层通过不能代替下层，也不能用模拟 150 条记录代替 150 台实机连接证据。

当前已有：App 编译、十四组现有检查、Mac 部分表单和导航检查、悬停显示检查、Windows 自包含打包。P2 阶段已实现：2048–4096 位 RSA 公钥范围检查（P2-03）、空值／错误类型拒绝（P2-04）、符号链接与路径越界检查（P2-05）、Windows 只读平台探测接口（P2-06）、Veyon 只读探测（P2-07，默认路径与服务注册检测，未安装显示“需要安装”而非报错；本机 Windows 10 基线：10.0.19044 x64，未安装 Veyon）。当前缺少：Windows 实机 UI、操作系统拖拽端到端、实际安装／账户／名称／重启／连接／失败恢复。新需求 150 台还没有改入代码。

只有改变了行为、发现新失败或存在未解决风险时才扩大／重复测试。纯文案和简单配色修改不要求新增镜像式单元测试；影响真实系统的逻辑必须有针对性失败测试。

## 2. 环境矩阵与记录模板

| 维度 | 首版必须覆盖 | 扩展或未知环境处理 |
| --- | --- | --- |
| 操作系统 | 至少一组确认的 Windows 10 x64 和 Windows 11 x64 | 记录具体构建号与版本；其余不自动列为已支持 |
| Mac | Apple Silicon 预览版启动、选择与拖放、表单 | 不验收真实 Windows 修改 |
| 权限 | 普通用户、管理员、UAC 取消、其他管理员提权 | 非预期身份不能修改错误目标账户 |
| Veyon | 未安装、同版、已知旧版、未知新版、损坏状态、非默认路径 | 未验证版本给明确限制 |
| 账户 | 本地普通／管理员、同名账户、禁用账户、域／Microsoft 身份 | 不支持的操作明确停止，不靠名字猜测 |
| 网络 | 无互联网、有局域网、局域网不可达 | 安装和连接验收分开 |
| 存储 | 本地、U 盘只读、中文和空格路径、磁盘不足、目录无权限 | UNC／网络盘若未支持须提前提示 |
| 屏幕 | 1366×768、1920×1080，100%／150%／200% 缩放 | 窗口控件不截断，滚动区域可访问 |
| 还原机制 | 已确认无还原的基线；现场存在时专门试装 | 未知不是“未安装”，必须重启复核 |
| 设备规模 | 1、99、100、150 条规划 | 实机与并发规模按实际覆盖数量单独填写 |

每次记录：

```text
测试 ID / 对应任务 ID：
日期 / 测试人：
App 版本 / Git 提交 / 产物 SHA-256：
Windows 版本、构建号、架构 / Veyon 版本：
账户类型 / 运行与提权方式：
是否加域 / 是否有还原软件（已确认或未知）：
前置状态与恢复方法：
操作步骤：
预期结果：
实际结果及系统读回证据：
日志位置（已脱敏）：
通过 / 失败 / 未执行 / 不适用：
缺陷编号 / 下一步：
```

“不适用”必须解释原因；不能把没时间执行记为不适用。恢复性测试在测试机／可恢复环境中进行，不拿生产课堂设备做故意破坏实验。

## 3. 基础界面与资料测试（P1–P2）

| ID | 场景 | 预期与验收重点 |
| --- | --- | --- |
| UI-01 | 启动学生页、切换教师页后返回 | 输入保留；未实现功能不可点击执行 |
| UI-02 | 鼠标悬停／按下各类按钮 | 深色背景不出现黑色文字；选中、非选中状态可区分 |
| UI-03 | Tab／Shift+Tab／Enter 操作 | 焦点可见、顺序合理、不意外触发敏感动作 |
| UI-04 | 小窗口、高 DPI、长路径、长校区名 | 无关键内容截断；按钮和错误可访问 |
| UI-05 | 选择资料后取消再次选择 | 保留原资料；不误清空、不启动部署 |
| UI-06 | 修改编号／前缀／操作选择 | 旧计划与旧预检失效，不能执行旧参数 |
| UI-07 | 快速重复点击检查／开始 | 同一任务只启动一次，状态一致 |
| PKG-01 | 原脚本生成的 BOM 配置＋合法公钥 | 兼容读取；不要求也不读取 admin.txt |
| PKG-02 | 拖入文件夹、拖入其中的 campus.json | 与选择按钮结果一致；只读取、不移动来源文件 |
| PKG-03 | 拖过按钮、移出区域、取消拖动 | 高亮准确清理，表单不被清空 |
| PKG-04 | 多文件、ZIP、普通文本、其他 JSON | 不接受错误类型；反馈可理解，不崩溃 |
| PKG-05 | 非法 JSON、错误类型、重复字段、未来 schema | 给明确错误，旧有效上下文不残留 |
| PKG-06 | 缺公钥、空文件、假 PEM、私钥、错误算法 | 不允许进入真正执行；不以有 BEGIN 标记代替有效性 |
| PKG-07 | 绝对路径、盘符、../、符号链接／重解析点 | 不能读取或执行包外资源 |
| PKG-08 | 预检后修改安装器／公钥或拔出 U 盘 | 执行前重新校验并停止，不沿用旧摘要 |
| PKG-09 | 非默认安装路径、中文和空格目录 | 不发生路径截断或参数误解释 |
| PKG-10 | 学生包含额外 admin.txt 或私钥 | 新包生成必须拒绝敏感混入；旧包读取不消费这些内容 |

编号测试：

- [ ] NAME-01：1→01、9→09、10→10、99→99、100→100、149→149、150→150。
- [ ] NAME-02：0、151、负数、小数、空输入、非 ASCII 数字、超长数值被拒绝。
- [ ] NAME-03：最终名称恰好 15 字符接受，16 字符拒绝；分别测试编号 99 和 100。
- [ ] NAME-04：教师清单与学生端生成的 150 个名称逐项一致；跨机房重复前缀产生冲突提示。
- [ ] NAME-05：StartNumber=149、Count=2 合法；Count=3 不可超过 150；单机房 151 条被拒绝。
- [ ] NAME-06：旧 PC-03 不被自动改成 PC-003；显示名称修改不改变连接地址。

## 4. 独立操作与组合测试（P3–P7）

在测试前记录本机名称、账户 SID 与组状态、Veyon 版本／配置和服务。验证“没有更改密码”应依靠调用边界检查与必要的人工登录验证，不能从系统读取旧密码来比较。

| ID | 选择项 | 必须发生 | 必须保持 |
| --- | --- | --- | --- |
| OP-01 | 只部署 Veyon | 安装／配置／验证所需组件 | 电脑名、账户、已有密码 |
| OP-02 | 只改电脑名 | 改名及待重启状态 | Veyon、账户、密码；无需部署包 |
| OP-03 | 只创建学生账户 | 新建明确指定的普通账户 | 管理员密码、电脑名、Veyon |
| OP-04 | 只修改指定管理员密码 | 修改选中 SID 对应的本地账户 | 其他账户、电脑名、Veyon |
| OP-05 | Veyon＋改名 | 按依赖执行两项，记录待重启 | 所有账户与密码 |
| OP-06 | Veyon＋创建学生账户 | 两项结果分开记录 | 管理员密码和电脑名 |
| OP-07 | 创建学生＋修改管理员密码 | 两项独立结果，改密明确后置 | Veyon 和电脑名 |
| OP-08 | 四项全选 | 逐项记录，失败后停止未开始修改 | 非计划账户和无关系统配置 |
| OP-09 | 不选任何项 | 禁止执行，说明需要选择操作 | 所有系统状态 |

所有 16 种布尔组合可以在计划层验证依赖与不越界；真实 Windows 测试至少覆盖上述独立与风险组合，再按发现的问题增加集成用例。不要为每种组合重复运行所有无关测试。

## 5. Windows 身份与权限（P2–P6）

- [ ] SEC-01：普通 UI 可浏览资料和生成计划；未开始修改时不要求 UAC。
- [ ] SEC-02：UAC 取消时系统无修改，UI 能继续编辑，不显示成功或永久忙碌。
- [ ] SEC-03：使用不同管理员提权时，仍针对原来明确选中的本地账户 SID，不误改提权人的密码。
- [ ] SEC-04：域账户与本地账户同名，不能仅通过名称匹配；不支持身份时阻断对应操作。
- [ ] SEC-05：已有 User 是管理员、禁用账户或有数据，不自动降权、启用、删除或重建。
- [ ] SEC-06：密码首尾空格保留原意；两次输入不一致和策略拒绝有明确提示，不打印实际密码。
- [ ] SEC-07：假 Worker、错误会话、其他用户进程、过大请求不能驱动高权限操作。
- [ ] SEC-08：参数含引号、分号、换行、PowerShell 特殊字符不变成命令；可执行文件路径不能由任意请求指定。
- [ ] SEC-09：部署包、日志、异常、运行记录、进程参数、回传消息无密码或私钥内容。
- [ ] SEC-10：文件选择和拖放在普通 UI 下可用；提权后不意外变成永远高权限运行的下载界面。

## 6. 安装、配置、教师端连接（P4、P7）

- [ ] VEY-01：未安装 → 离线安装 → 读回版本和组件；学生端无不需要的 Master，教师端具备 Master。
- [ ] VEY-02：同版正常安装重复运行不破坏配置；同版损坏进入明确修复计划。
- [ ] VEY-03：已知旧版先备份后升级；未知新版不自动降级或全量覆盖。
- [ ] VEY-04：安装器出错、被锁、返回待重启，不误当作一般成功；显示后续步骤为何停止或延期。
- [ ] VEY-05：公钥导入失败或同名异指纹，不改成其他校区，不自动删除重建密钥。
- [ ] VEY-06：服务启动验证有超时与错误信息；端口不可达与服务未启动能区分。
- [ ] VEY-07：仅修改需要的防火墙规则，无全局关闭；已有无关规则保持。
- [ ] TEA-01：创建与复用校区密钥分别测试；导出失败后重试不创建第二套身份。
- [ ] TEA-02：日常教师账户能读对应私钥并使用 Master，不依赖持续管理员运行。
- [ ] TEA-03：目录导入 150 条、重复导入、部分冲突、自定义地址和中文显示名均可核对。
- [ ] TEA-04：学生包含 App 所需运行文件、资源和公钥，未混入真实凭据、教师私钥、运行日志。
- [ ] TEA-05：教师新生成的包在学生 App 读取，不再需要依靠旧 teacher 脚本补步骤。
- [ ] NET-01：本地安装完成与连接验收分开；无互联网但局域网互通时可连接。
- [ ] NET-02：普通教师身份查看／控制学生机，双方重启后再次验证。
- [ ] NET-03：名称解析失败但 IP 可达时提示地址问题；主机名／IP 模式的限制写清楚。

## 7. 故障注入与恢复（P3、P8）

故障注入先使用假执行器或受控测试资源，再在可恢复 Windows 环境验证。每例记录最后完成步骤和实际系统状态。

| ID | 注入位置 | 预期结果 |
| --- | --- | --- |
| FAIL-01 | 第一次修改前预检失败 | 无系统修改，不创建误导性成功记录 |
| FAIL-02 | 备份目录无权限或磁盘满 | 必须备份的修改不开始，明确原因 |
| FAIL-03 | 安装器非零返回／超时 | 停止依赖步骤，保留输出摘要和状态 |
| FAIL-04 | 安装完成后公钥配置失败 | 显示部分完成，说明 Veyon 已安装 |
| FAIL-05 | 配置完成后服务启动失败 | 不能显示可连接；允许诊断与有界重试 |
| FAIL-06 | 改名完成后后续步骤失败 | 明确名称已更改／待重启，不隐去事实 |
| FAIL-07 | 密码修改后日志落盘失败 | 标为需核对，不盲目重复改密或声称已回滚 |
| FAIL-08 | UI 退出／Worker 断连 | 展示或恢复到真实状态；不能启动第二个冲突任务 |
| FAIL-09 | 系统修改与结果记录之间断电 | 重启后重新读回系统，识别已完成步骤 |
| FAIL-10 | 用户在长操作中取消／关闭窗口 | 在安全点停止；不随意强杀安装器 |
| FAIL-11 | 备份恢复遇到新配置变更 | 先比较并提示冲突，不覆盖后来人工配置 |
| FAIL-12 | 拔 U 盘或替换资料 | 不执行未验证资源；已完成步骤有记录 |
| FAIL-13 | 重复运行同一包／新包 | 不重复创建密钥、目录或账户；不因重试自动改密 |
| FAIL-14 | 还原机制在重启后恢复旧状态 | 报告实际回退，停止扩大部署，现场核对维护模式 |

## 8. 性能与 150 台容量

- [ ] PERF-01：生成并校验 150 条记录，计数、唯一性、排序和名称正确。
- [ ] PERF-02：列表滚动、编辑、筛选与导入不冻结 UI；长任务有进度和取消路径。
- [ ] PERF-03：记录测试机型号、数据规模、耗时与内存；初步建议 150 条纯本地规划在 1 秒内完成，需在 P0 测试机确认后作为阈值，尚非性能承诺。
- [ ] PERF-04：安装耗时按机器与安装器实测记录，不设虚假的统一秒数承诺。
- [ ] PERF-05：实际试点数量与逻辑容量分别记录；不足 150 台实测时明确范围，不声称“150 台全部验证”。
- [ ] PERF-06：Veyon 同屏查看性能独立记录，不把 App 目录容量当作 Veyon 远控性能保证。

## 9. 分发专用验收（P10–P11 才执行）

- [ ] DIST-01：教师开启服务，另一台真实电脑浏览下载；教师端选定网卡地址正确。
- [ ] DIST-02：同一 Wi-Fi 但开启隔离时失败提示准确；不把“同 Wi-Fi”当作互通保证。
- [ ] DIST-03：端口冲突、IP 变化、多网卡、教师休眠后恢复均有可理解结果。
- [ ] DIST-04：未联网时页面全部正常，不依赖 CDN、在线字体或云 API。
- [ ] DIST-05：只能下载当前明确发布的包，路径穿越、任意文件读取、目录列表和执行请求被拒绝。
- [ ] DIST-06：链接／会话到期、未授权访问、限流按规则处理；日志不暴露凭据或完整秘密令牌。
- [ ] DIST-07：中断下载不产生“可安装”的残缺文件；恢复后摘要与版本一致。
- [ ] DIST-08：下载过程中发布新版本，已有下载仍对应原版本快照。
- [ ] DIST-09：停止服务后端口释放，临时规则按设计清理，不影响 Veyon 服务规则。
- [ ] DIST-10：按 1、10 和计划并发上限测试；150 台分批需求可完成，教师机内存不随包大小×人数增长。
- [ ] DIST-11：下载安装完成但未运行 App，教师端只能显示下载信息，不能显示部署成功。
- [ ] DIST-12：如实现结果回传，校验身份、重复、伪造、离线重试和本地结果独立性。
- [ ] DIST-13：来源验证机制实测；同一未认证页面给出的哈希不算独立的可信身份凭证。

## 10. 试点顺序与停止条件

| 批次 | 范围 | 进入下一批的条件 |
| --- | --- | --- |
| T0 | 可恢复 Windows 实验环境 | 规则、权限、故障与单项操作通过 |
| T1 | 一教师＋一学生 | 双向验收、重启、账户人工登录和恢复路径通过 |
| T2 | 5–10 台 | 个体差异可解释、无误改、日志足够诊断 |
| T3 | 一个真实机房，最多 150 台 | 逐批完成，设备台账和问题记录完整 |
| T4 | 其他机房／不同环境 | 在既有矩阵之外新增环境，重新做必要兼容性验证 |

发生下列任一项立即停止扩大范围：改错账户、非选中操作发生修改、凭据泄露、错误密钥导致原环境失联、不可解释的数据损失、失败却显示整体成功。先修复并验证，再决定恢复试点。

停止扩大部署不等于自动撤销已完成修改。现场根据每台运行记录和已核对备份分别恢复。

## 11. 发布前检查

- [ ] REL-01：任务状态与验收证据一致；未完成项目出现在已知限制里，而不是隐藏在按钮后。
- [ ] REL-02：Windows 10/11 支持矩阵完整，App／Veyon／运行库版本有记录；不支持的架构提前提示。
- [ ] REL-03：核心版四种独立操作和必要组合通过；未选操作没有副作用。
- [ ] REL-04：公开包内无密码、私钥、真实机器清单或敏感日志；演示资料明确是示例。
- [ ] REL-05：包内含离线运行所需依赖；在未装 SDK/.NET 的测试环境验证。
- [ ] REL-06：App 文件版本、窗口显示、说明、manifest 和 ZIP 名称一致；每次发布新版本名，不覆盖旧版本。
- [ ] REL-07：安装资源来源、摘要和所采用签名验证记录齐备；依赖和再分发材料已核对。
- [ ] REL-08：干净构建可重复，构建命令、SDK、依赖、提交记录和产物摘要已保存。
- [ ] REL-09：ZIP 完整性测试通过；完整解压后从非默认目录启动，不能只在开发输出目录运行。
- [ ] REL-10：文档链接、当前能力描述、截图和任务状态一致；文本校验清单更新并验证。
- [ ] REL-11：课堂日常教师身份完成连接验收；重启后结果可复核。
- [ ] REL-12：上一稳定版本、恢复说明和问题反馈入口可用；升级和卸载边界清楚。
- [ ] REL-13：本地核心版不依赖分发模块；分发版本另外满足 DIST 清单。
- [ ] REL-14：发布负责人记录结论与残余风险；严重缺陷未关闭时不标为正式交付。

## 12. 缺陷分级与完成定义

| 等级 | 示例 | 处理要求 |
| --- | --- | --- |
| S0 | 改错账户、敏感资料外泄、执行任意命令、数据损失 | 停止相关操作和扩大部署，修复后重新审查边界 |
| S1 | 不能安装、错误成功状态、无法登录或连接、恢复记录缺失 | 阻断对应功能正式交付 |
| S2 | 有清楚绕行办法的非核心失败、个别路径或显示问题 | 明确范围、修复计划与已知限制 |
| S3 | 不影响操作理解的间距、文案或装饰问题 | 归入后续体验修复，不伪装成核心阻断 |

单项完成定义：代码实现、相关正常与失败验证、日志／错误行为、文档与状态更新齐备。阶段完成定义：前置阶段条件和本阶段所有发布必需项通过；延期项目说明不影响当前交付目标。

模板化测试名、固定 PASS 字样和截图不能替代真实检查；对系统修改必须能说明检查了哪个系统状态以及得到什么结果。
